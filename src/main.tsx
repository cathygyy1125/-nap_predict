import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import EBNapProbabilityCalculator from '../eb_nap_probability';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <EBNapProbabilityCalculator />
  </React.StrictMode>
);
